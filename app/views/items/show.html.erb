<h1> <%= @item.title %></h1>

<p><strong>Categories:</strong> <%= format_categories @item.categories %></p> 

<%= image_tag @item.image.medium.url %>
<p>
  <%= link_to "Download", @item.image.url, download: true %>
  </p>
